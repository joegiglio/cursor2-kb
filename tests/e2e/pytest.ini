[pytest]
base_url = http://localhost:5000
#addopts = --headed --browser chromium -v --slowmo 1000 --screenshot on --output test-results
addopts = --headed --browser chromium -v --slowmo 1000
markers =
    smoke: mark a test as part of the smoke test suite
    regression: mark a test as part of the regression test suite